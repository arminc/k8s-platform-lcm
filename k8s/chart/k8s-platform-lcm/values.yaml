# Default values for k8s-platform-lcm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: arminc/lcm
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "0.13.0"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  name: ""
  annotations: {}

clusterRole:
  annotations: {}

deployAnnotations: {}

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

# influence the cluster role permissions, helm requires the ability to read secrets
imageScan:
  enabled: true
helmScan: 
  enabled: false # secrets being accessible may be a non-starter for folks


config: |-
  type: yml
restart:
  enabled: true
  schedule: "8 */1 * * *"
  image:
    repository: bitnami/kubectl
    tag: ""